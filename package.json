{
	"name": "light-launcher",
	"version": "1.0.0",
	"private": true,
	"description": "Ultra-lightweight, customizable Linux gaming launcher powered by Go & Electron",
	"main": "dist/main/index.js",
	"scripts": {
		"dev:go": "go build -o bin/light-launcher-instance ./core/cmd/instance",
		"dev:renderer": "cd src/renderer && bun run dev",
		"dev:electron": "tsc && sleep 1 && electron .",
		"dev": "bun run dev:go && concurrently \"bun run dev:renderer\" \"bun run dev:electron\"",
		"build:go": "go build -ldflags=\"-s -w\" -o bin/light-launcher-instance ./core/cmd/instance",
		"build:renderer": "cd src/renderer && bun run build",
		"build:electron": "tsc",
		"build": "bun run build:go && bun run build:renderer && bun run build:electron",
		"dist": "bun run build && electron-builder --config build/electron-builder.yml",
		"typecheck": "tsc --noEmit && cd src/renderer && svelte-check --tsconfig ./tsconfig.json",
		"format": "oxfmt --write .",
		"format:check": "oxfmt --check .",
		"lint": "oxlint",
		"lint:fix": "oxlint --fix",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage"
	},
	"dependencies": {},
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "^6.2.4",
		"@tsconfig/svelte": "^5.0.6",
		"@types/node": "^25.6.0",
		"@vitest/coverage-v8": "^4.1.10",
		"concurrently": "^9.1.2",
		"electron": "^34.3.0",
		"electron-builder": "^25.1.8",
		"oxfmt": "^0.63.0",
		"oxlint": "^1.78.0",
		"sass": "^1.97.3",
		"svelte": "^5.49.1",
		"svelte-check": "^4.3.6",
		"svelte-preprocess": "^6.0.3",
		"tslib": "^2.8.1",
		"typescript": "^5.9.3",
		"vite": "^7.3.1",
		"vitest": "^4.1.10"
	},
	"trustedDependencies": [
		"@parcel/watcher",
		"svelte-preprocess"
	]
}
